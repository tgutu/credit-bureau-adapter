# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: CreditBureauAdapterService API
    version: 0.0.1
paths:
    /v1/credit/bureaus:
        get:
            tags:
                - CreditBureauAdapterService
            description: |-
                Retrieves a list of supported credit bureaus.
                 No parameters required.
                 Returns a list of credit bureaus with their IDs and names.
            operationId: GetBureaus
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetBureausResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/credit/report:
        post:
            tags:
                - CreditBureauAdapterService
            description: |-
                Retrieves a detailed credit report for an individual.
                 Requires personal identification details.
                 Returns a comprehensive credit report including credit history, accounts, and inquiries.
            operationId: GetCreditReport
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetCreditReportRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetCreditReportResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/credit/score:
        post:
            tags:
                - CreditBureauAdapterService
            description: |-
                Retrieves the credit score for an individual.
                 Requires personal identification details.
                 Returns the credit score as an integer value.
            operationId: GetCreditScore
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetCreditScoreRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetCreditScoreResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        Account:
            type: object
            properties:
                accountType:
                    type: string
                accountNumber:
                    type: string
                status:
                    type: string
                balance:
                    type: string
                creditLimit:
                    type: string
                openedDate:
                    type: string
                closedDate:
                    type: string
        Bureau:
            type: object
            properties:
                name:
                    type: string
        GetBureausResponse:
            type: object
            properties:
                bureaus:
                    type: array
                    items:
                        $ref: '#/components/schemas/Bureau'
            description: Response message for GetBureaus RPC.
        GetCreditReportRequest:
            type: object
            properties:
                firstName:
                    type: string
                lastName:
                    type: string
                dateOfBirth:
                    type: string
                nationalId:
                    type: string
                bureauName:
                    type: string
            description: Request message for credit report and score retrieval.
        GetCreditReportResponse:
            type: object
            properties:
                reportId:
                    type: string
                fullName:
                    type: string
                dateOfBirth:
                    type: string
                accounts:
                    type: array
                    items:
                        $ref: '#/components/schemas/Account'
                inquiries:
                    type: array
                    items:
                        $ref: '#/components/schemas/Inquiry'
                publicRecords:
                    type: array
                    items:
                        $ref: '#/components/schemas/PublicRecord'
                reportDate:
                    type: string
            description: Response message for credit report retrieval.
        GetCreditScoreRequest:
            type: object
            properties:
                firstName:
                    type: string
                lastName:
                    type: string
                dateOfBirth:
                    type: string
                nationalId:
                    type: string
                bureauName:
                    type: string
            description: Response message for credit score retrieval.
        GetCreditScoreResponse:
            type: object
            properties:
                fullName:
                    type: string
                dateOfBirth:
                    type: string
                creditScore:
                    type: integer
                    format: int32
                scoreDate:
                    type: string
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Inquiry:
            type: object
            properties:
                inquiryType:
                    type: string
                date:
                    type: string
                institution:
                    type: string
        PublicRecord:
            type: object
            properties:
                recordType:
                    type: string
                description:
                    type: string
                dateFiled:
                    type: string
                status:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: CreditBureauAdapterService
